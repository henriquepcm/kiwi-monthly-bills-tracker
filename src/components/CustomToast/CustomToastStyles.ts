import { ToastContainer } from "react-toastify";
import styled from "styled-components";

export const StyledCustomToastContainer = styled(ToastContainer)`
     .Toastify__progress-bar-theme--dark {
          background: var(--kiwi-color6);
     }
`;
